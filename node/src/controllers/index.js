
var Controllers = {
    main: require('./main'),
    tickets: require('./tickets'),
    messages: require('./messages'),
    servers: require('./servers'),
    api: require('./api')
};

module.exports = Controllers;
