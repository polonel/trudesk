<div class="small-12 large-10 columns nopadding typesContent" ng-controller="settingsCtrl">
    <div class="page-title noshadow" style="padding-left: 25px;">
        <p class="left">{{title}}</p>
        <div class="uk-float-right">
            <a href="#" class="no-ajaxy md-btn md-btn-success" style="margin-top: 15px;" ng-click="showCreateTicketTypeWindow($event)">Create</a>
        </div>
    </div>

    <div class="full-height page-content no-border-top" style="position: relative;">
        <div class="ticketTypeList full-height">
            <table id="ticketTypesTable" class="pDataTable stripe">
                <thead>
                <tr>
                    <th></th>
                    <th>Name</th>
                    <!--<th style="text-align: right; padding-right: 25px;">Ticket Count</th>-->
                </tr>
                </thead>
                <tbody>
                {{#each data.types}}
                    <tr ng-click="editTicketType($event)" data-tickettypeoid="{{_id}}">
                        <td>
                            <!--<input id="c_{{_id}}" type="checkbox" class="poloCheckbox"/>-->
                            <!--<label for="c_{{_id}}"></label>-->
                        </td>
                        <td>{{name}}</td>
                    </tr>
                {{/each}}
                </tbody>
            </table>
        </div>
    </div>

    {{> createTicketTypeWindow}}
</div>

{{#contentFor 'js-plugins'}}

{{/contentFor}}