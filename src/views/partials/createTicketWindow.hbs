<div class="uk-modal" id="ticketCreateModal" ng-controller="ticketsCtrl">
    <div class="uk-modal-dialog">
        <form class="uk-form-stacked" id="createTicketForm" action="#" method="POST">
            <input type="hidden" name="socketId" id="socketId" value="{{data.user.socket.id}}" />
            <div class="uk-margin-medium-bottom">
                <label for="subject">Subject</label>
                <input type="text" class="md-input" id="subject" name="subject"/>
            </div>
            <div class="uk-margin-medium-bottom">
                <label for="issue">Description</label>
                <textarea class="md-input" id="issue" name="issue"></textarea>
            </div>
            <div class="uk-margin-medium-bottom">
                <label for="group" class="uk-form-label">Group</label>
                <select class="uk-form-width-full selectize-white" name="group" id="group" data-md-selectize-inline>
                    {{#each data.common.groups}}
                        <option value="{{{_id}}}">{{name}}</option>
                    {{/each}}
                </select>
            </div>
            <div class="uk-margin-medium-bottom clearfix">
                <div class="left" style="padding-right: 10px; width: 30%;">
                    <label for="type" class="uk-form-label">Type</label>
                    <select class="selectize-white" name="type" id="type" data-md-selectize-inline>
                        {{#each data.common.ticketTypes}}
                            <option value="{{{_id}}}">{{name}}</option>
                        {{/each}}
                    </select>
                </div>

                <div class="left" style="width: 70%;">
                    <label for="tags" class="uk-form-label">Tags</label>
                    <select name="tags" id="tags" class="chosen-select1 selectize-white" multiple data-md-selectize-inline data-md-selectize-bottom="true">
                        {{#each data.common.ticketTags}}
                            <option id="TAG__{{_id}}" value="{{_id}}">{{name}}</option>
                        {{/each}}
                    </select>
                </div>



            </div>
            <div class="uk-margin-medium-bottom">
                <label for="ticket_priority" class="uk-form-label">Priority</label>
                <div>
                            <span class="icheck-inline">
                                <input class="with-gap" type="radio" name="priority" id="priority_minor" value="1" data-md-icheck checked />
                                <label for="priority_minor" class="inline-label"><span class="uk-badge uk-badge-success">Normal</span></label>
                            </span>
                            <span class="icheck-inline">
                                <input class="with-gap" type="radio" name="priority" id="priority_critical" value="2" data-md-icheck />
                                <label for="priority_critical" class="inline-label"><span class="uk-badge uk-badge-warning">Urgent</span></label>
                            </span>
                            <span class="icheck-inline">
                                <input class="with-gap" type="radio" name="priority" id="priority_blocker" value="3" data-md-icheck />
                                <label for="priority_blocker" class="inline-label"><span class="uk-badge uk-badge-danger">Critical</span></label>
                            </span>
                </div>
            </div>
            <div class="uk-modal-footer uk-text-right">
                <button type="button" class="md-btn md-btn-flat uk-modal-close">Close</button>
                <button type="button" class="md-btn md-btn-flat md-btn-flat-primary" id="submitNewTicket" ng-click="submitTicketForm()">Create Ticket</button>
            </div>
        </form>
    </div>
</div>